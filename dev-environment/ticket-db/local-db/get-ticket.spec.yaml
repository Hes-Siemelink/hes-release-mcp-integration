Input parameters:
  id:
    description: Full ticket identifier (e.g. TICKET-007)

Print: Getting ticket ${id}

SQLite:
  file: /app/tickets.db
  query: |
    SELECT
      *
    FROM ticket
    WHERE id = '${id}'  
